/*
 * 이 파일은 main.c 통합 예시 메모입니다.
 * CubeMX가 생성한 main.c에서 아래 순서를 유지하세요.
 *
 * int main(void)
 * {
 *   HAL_Init();
 *   SystemClock_Config();
 *   MX_GPIO_Init();
 *   MX_USART1_UART_Init();
 *   MX_SPI1_Init();
 *   MX_SPI2_Init();
 *   MX_FDCAN1_Init();
 *   MX_FREERTOS_Init();
 *   osKernelStart();
 *   while (1) { }
 * }
 */
